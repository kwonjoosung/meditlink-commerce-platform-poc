# CLAUDE.md

이 프로젝트는 E-Commerce 플랫폼을 구축해보는 PoC 입니다.
DDD 기반 모듈러 모놀리스로 설계되었으며, Java + Spring Boot + PostgreSQL + Stripe를 사용합니다.

---

## 규칙

### 1. 언어

- **코드를 제외한 모든 응답과 결과물은 한글로 제공한다.**
- 코드 내 주석도 한글 허용 (단, 변수명/메서드명은 영문).
- 커밋 메시지, PR 설명 등도 한글.

### 2. 페어 개발

이 프로젝트는 사람과 AI 에이전트가 페어로 개발한다. 아래 규칙을 반드시 따른다.

**계획 먼저 (Plan First)**
- 구현 전 반드시 계획을 정의하고 승인받는다.
- 계획에는 파일 목록, 구현 순서, 핵심 결정 사항을 포함한다.
- 승인 없이 구현하지 않는다.

**모르면 물어보기 (Ask, Don't Assume)**
- 설계/정책 결정이 필요한 사항은 임의로 판단하지 않고 질문한다.
- 문서에 명시되지 않은 사항은 반드시 확인한다.
- "보통 이렇게 합니다" 또는 "일반적으로는" 같은 이유로 임의 결정하지 않는다.
- 확신이 없으면 선택지를 제시하고 결정을 요청한다.

**작게 나누기 (Small Steps)**
- 한 번에 하나의 Aggregate 또는 하나의 기능만 구현한다.
- 큰 작업은 단계를 나눠 제시하고 하나씩 진행한다.

**피드백 루프**
```
에이전트: 계획 제시 → 사람: 승인/수정 → 에이전트: 구현 → 사람: 리뷰 → (반복)
```

### 3. 메모리 관리

- 대화 중 결정된 사항, 합의 내용, 중요한 맥락은 `.md` 파일로 저장한다.
- 저장 위치: `doc/` 디렉토리 하위.
- 파일명은 내용을 알 수 있게 명명한다 (예: `doc/DECISIONS.md`, `doc/MEMO-pricing-policy.md`).
- 이전 대화의 맥락이 필요하면 해당 `.md` 파일을 읽어서 참고한다.
- 설계 문서와 구현이 달라지면 문서를 업데이트한다.

### 4. 코드 작성

- 이 프로젝트의 설계 문서는 `doc/INDEX.md`에서 네비게이션할 수 있다.
- 구현 전 반드시 관련 설계 문서를 읽고 시작한다.
- 설계 문서와 충돌하는 구현은 하지 않는다. 충돌이 발견되면 먼저 논의한다.
- 테스트 코드를 함께 작성한다.

---

## 설계 문서

모든 설계 문서는 `doc/` 디렉토리에 있다.
자세한 목록과 설명은 [doc/INDEX.md](./doc/INDEX.md) 참조.

---

## 기술 스택

| 항목 | 기술                    |
|------|-----------------------|
| 언어 | Java 25+              |
| 프레임워크 | Spring Boot 4.x       |
| DB | PostgreSQL (JSONB 활용) |
| PG | Stripe (Java SDK)     |
| 빌드 | Gradle                |
| 테스트 | JUnit 6, ArchUnit     |
